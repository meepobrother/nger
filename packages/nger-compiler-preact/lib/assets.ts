import { NgModuleBootstrap, NgModuleRef } from 'nger-core';
// 将ng模板编译成preact可以执行的文件
export class NgerCompilerPreactAssets extends NgModuleBootstrap {
    async run(ref: NgModuleRef<any>) { 
        
    }
}
