```ts
createRender(metadata: any){
  return (h:any) => h(name,{
    ...metadata.inputs,
    ...metadata.outputs,
    ...metadata.attributes,
    ...metadata.variables,
    ...metadata.references,
  },...metadata.children.map(me=> createRender(me)(h) ))
}
```
